"""
Agent package — stub (modules removed during architecture cleanup).

The original agent modules (tools.py, local_runner.py) were deleted
during the STEP 11/12 architecture refactoring. This package exists
for backwards compatibility but contains no runnable agent code.
"""
