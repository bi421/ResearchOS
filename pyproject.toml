[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "researchos"
version = "1.0.1"
description = "Institutional-grade market research platform - deterministic, explainable, scientific"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "ResearchOS Scientific License"}
authors = [
    {name = "ResearchOS Team"},
]
keywords = [
    "market-research",
    "institutional-trading",
    "macro-research",
    "deterministic",
    "explainable-ai",
    "scientific-method",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "License :: Other/Proprietary License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering",
    "Topic :: Office/Business :: Financial",
]
dependencies = [
    "pandas>=2.0",
    "numpy>=1.24",
    "polars>=1.0",
    "pydantic>=2.0",
]

[project.optional-dependencies]
cli = [
    "click>=8.0",
]
api = [
    "fastapi>=0.100.0",
    "uvicorn>=0.20.0",
]
dev = [
    "pytest>=7.0",
    "pytest-cov>=4.0",
]

[project.urls]
Homepage = "https://github.com/researchos/researchos"
Documentation = "docs/"

[tool.setuptools.packages.find]
where = ["."]
include = ["researchos*"]

[tool.pytest.ini_options]
# Mirrors the CI suite (.github/workflows/test-python.yml):
#   pytest researchos/tests/ tests/unit/ + package-local test roots
testpaths = [
    "researchos/tests",
    "tests/unit",
    "researchos/data_engine/tests",
    "researchos/market_memory/tests",
    "researchos/experiments/phase51/tests",
]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = "-v --tb=short --ignore=researchos/tests/test_ensemble.py --ignore=researchos/tests/test_sp500.py --ignore=researchos/tests/test_strategies_real.py --ignore=researchos/tests/test_tpsl.py --ignore=researchos/tests/test_tpsl_grid.py --ignore=researchos/tests/test_cpp_backtest_regression.py"

[tool.mypy]
python_version = "3.10"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
disallow_incomplete_defs = true

[tool.ruff]
exclude = ["archive"]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W"]
ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
# Scripts run sys.path bootstrap before imports, so E402 is allowed
"scripts/**" = ["E402"]
"src/**" = ["E402"]
"researchos/tests/**" = ["E402"]

[tool.ruff.format]
quote-style = "double"
line-ending = "auto"
