The M1 event engine intentionally reuses the project's deterministic indicator primitives. It creates a distinct M1 event surface instead of changing the existing D1 extractor, preserving compatibility with existing D1 research.

The outcome engine remains the single forward-label implementation. The M1 contract defines the label semantics and is designed to be consumed by the real-data integration layer next.
