# Next real-data gate

The next implementation must consume the audited MT5 XAUUSD M1 dataset. It must not synthesize missing observations or substitute another gold instrument.

Required output is a reproducible event/outcome artifact containing dataset identity, event contract, event timestamp, actual realized endpoint, directional label, and provenance sufficient for leakage review.
